[phases.setup]
nixPkgs = ["tesseract"]

[phases.install]
cmds = [
    "apt-get update || true",
    "apt-get install -y tesseract-ocr tesseract-ocr-eng || true",
    "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
